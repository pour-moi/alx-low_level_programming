#include <stdio.h>

/**
 * main - prints command line
 * @argc: unused argument
 * @argv: the string
 *
 * Return: Always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}

